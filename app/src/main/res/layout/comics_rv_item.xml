<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/comics_poster_iv"
        android:layout_width="115dp"
        android:layout_height="145dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="@string/hero_comics_poster_description"
        android:scaleType="centerCrop"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/place_holder" />

    <TextView
        android:id="@+id/comics_title_tv"
        style="@style/WhiteSmallTextTheme"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="2dp"
        android:layout_marginTop="6dp"
        android:layout_marginEnd="2dp"
        android:gravity="center"
        android:maxLines="3"
        android:minLines="2"
        android:text="@string/hero_item_small_description"
        app:layout_constraintEnd_toEndOf="@+id/comics_poster_iv"
        app:layout_constraintStart_toStartOf="@+id/comics_poster_iv"
        app:layout_constraintTop_toBottomOf="@+id/comics_poster_iv" />
</androidx.constraintlayout.widget.ConstraintLayout>